// import { createReducer } from '@reduxjs/toolkit';
// import contactsList from 'backEnd_imitation/contacts';
// import { addContacts, deleteContact, setStatusFilter } from './actions';

// const contactInitialState = [...contactsList];

// const filterInitialState = '';

// export const contactsListReducer = createReducer(contactInitialState, {
//   [addContacts]: (state, { payload }) => state.push(payload),

//   [deleteContact]: (state, { payload }) =>
//     state.filter(contact => contact.id !== payload),
// });

// export const filterReducer = createReducer(filterInitialState, {
//   [setStatusFilter]: (state, { payload }) => payload,
// });
